<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="clinicEntity"
            type="edu.metrostate.ics372.thatgroup.clinicaltrial.models.entities.ClinicEntity" />

        <variable
            name="callback"
            type="edu.metrostate.ics372.thatgroup.clinicaltrial.ui.callback.EntityClickCallback" />
    </data>

    <android.support.v7.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/item_horizontal_margin"
        android:layout_marginStart="@dimen/item_horizontal_margin"
        android:minHeight="@dimen/item_min_height"
        android:onClick="@{() ->  callback.onClick(clinicEntity)}"
        android:orientation="horizontal"
        app:cardUseCompatPadding="true">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/item_horizontal_margin"
            android:layout_marginStart="@dimen/item_horizontal_margin">

            <TextView
                android:id="@+id/clinic_id"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{clinicEntity.id}" />

            <TextView
                android:id="@+id/clinic_trialId"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_marginEnd="5dp"
                android:text="@{clinicEntity.trialId}" />

            <TextView
                android:id="@+id/clinic_name"
                android:layout_width="333dp"
                android:layout_height="wrap_content"
                android:layout_alignParentStart="true"
                android:layout_below="@+id/clinic_id"
                android:text="@{clinicEntity.name}" />
        </RelativeLayout>

    </android.support.v7.widget.CardView>
</layout>